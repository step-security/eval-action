{
  "private": true,
  "packageManager": "npm@11.13.0",
  "workspaces": [
    "eval",
    "test-eval"
  ],
  "scripts": {
    "build": "turbo run build",
    "watch": "turbo run watch",
    "dev": "turbo run dev",
    "start": "turbo run start"
  },
  "dependencies": {},
  "devDependencies": {
    "turbo": "^2.6.1"
  }
}
